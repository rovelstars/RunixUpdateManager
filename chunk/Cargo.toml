[package]
name = "runix-chunk"
version = "0.1.0"
edition = "2024"
description = "Content-defined chunking + content-addressed sync for RunixOS image updates"

[dependencies]
fastcdc = "3"
sha2 = "0.10"
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
