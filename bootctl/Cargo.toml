[package]
name = "runix-bootctl"
version = "0.1.0"
edition = "2024"
description = "A/B boot-control state + slot selection for RunixOS, shared by RUM (std) and Rignite (no_std). No deps, fixed binary on-disk format."

[[bin]]
name = "rignite-sim"
path = "src/bin/rignite_sim.rs"

[dependencies]
